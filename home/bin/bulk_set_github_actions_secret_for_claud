#!/bin/bash
set -euo pipefail

echo 'cd to ~/bin'
cd ~/bin

echo 'Exec all for each repository'
./set_github_actions_secret_for_claud bannzai/Focus
./set_github_actions_secret_for_claud bannzai/bigquery-analytics-to-github-issue
